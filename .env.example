# Example environment file for Porkbun SSL downloader
# Copy this file to .env and fill in your values

# Required: Your domain(s) - comma-separated for multiple domains
DOMAIN=example.com,www.example.com

# Required: Your Porkbun API credentials
# Get them from: https://porkbun.com/account/api
API_KEY=pk1_your_api_key_here
SECRET_KEY=sk1_your_secret_key_here

# Required: Cron schedule for automatic renewal
# The application runs as a daemon on the specified schedule
# Examples:
#   Daily at 2 AM: 0 2 * * *
#   Every 12 hours: 0 */12 * * *
#   Weekly on Sunday at 3 AM: 0 3 * * 0
CRON_SCHEDULE=0 2 * * *

# Optional: Customize API endpoint (default shown)
# API_URL=https://api.porkbun.com/api/json/v3

# Optional: Customize certificate paths (default shown)
# Use {domain} placeholder for multiple domains
# CERTIFICATE_PATH=/certs/{domain}/certificate.pem
# PRIVATE_KEY_PATH=/certs/{domain}/private_key.pem
# COMBINED_CERT_PATH=/certs/{domain}/combined.pem